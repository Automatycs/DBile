#include "../include/Table.hpp"
#include "../include/Database.hpp"
#include <iostream>
#include <algorithm>

using namespace std;
/*
int main(int argc, char **argv)
{

    vector<string> v;
    v.push_back("col 1");
    v.push_back("col 2");
    Database db = Database("mybasededonnee");
    Database nulle = Database("nulle");

    Table t1 = Table("test", db, v);
    Table t2 = Table("aaa", db, v);
    Table t3 = Table("hyaaaa", db, v);
    db.show_tables();
    Database::show_databases();
    
    Table *aaa = db.find("aaa");
    aaa->insert({"a","b"});
    aaa->insert({"c","b"});
    aaa->insert({"d","b"});
    aaa->insert({"e","b"});
    aaa->insert({"f","b"});
    //aaa->select({"*"});
    aaa->select({"col 1"});
    return 0;
}
*/